import React, { Component } from 'react';

class AddNewSuccessButton extends Component {
    render() {
        return (
            <button>Dodaj</button>
        );
    }
}

export default AddNewSuccessButton;